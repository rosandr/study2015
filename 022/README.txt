Повторение задания 012 writer/readers,
но с доступом к контейнеру чт.зап с помощью разделяемой памяти.

1. run ./make.sh;
2. launch shmvect (for example: ./shmvect 20 30 40) in one xterm (exit: Ctrl-C);
3. cd monitor and run ./make.sh;
4. launch shmmon(for example: ./shmmon) in one another xterm (exit: Ctrl-C);
5. follow instructions.
